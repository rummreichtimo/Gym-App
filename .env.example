# SQLite by default - zero setup. Swap for a PostgreSQL URL to deploy.
DATABASE_URL="file:./dev.db"

# Secret used to sign session cookies. MUST be changed in production.
AUTH_SECRET="change-me-to-a-long-random-string"
